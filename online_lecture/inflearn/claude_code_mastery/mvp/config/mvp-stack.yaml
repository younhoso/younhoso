# MVP 기술 스택 설정

level: MVP

frontend:
  html: HTML5
  css: CSS3
  js: Vanilla JS (ES6+)

backend:
  provider: Supabase
  features:
    - Database (PostgreSQL)
    - Authentication
    - Row Level Security

deployment:
  frontend: GitHub Pages
  backend: Supabase (managed)
