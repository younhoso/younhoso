'use client';

import styled from 'styled-components';

export const BannerStyled = styled.div`
  .banner-inner {
    width: 100%;
  }

  &.benefit {
    > div {
      > div {
        height: auto;
      }
    }
  }
`;
