'use client';

import styled from 'styled-components';

export const ProductReviewMobileStyled = styled.div`
  .detail-page-orderBy {
    .select-wrapper {
      padding: 10px 16px 9px 16px;
    }
  }
`;
