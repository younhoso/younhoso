'use client';

import styled from 'styled-components';

export const OrderMethodStyled = styled.div`
  .order-method-children {
    display: flex;
    gap: 8px;
    .Button {
      flex: 1 1 0;
    }
  }

  .no-border-bottom {
    border-bottom: none;
  }
`;
